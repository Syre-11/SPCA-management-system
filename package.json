{
  "name": "spca-management-system",
  "private": true,
  "scripts": {
    "embed:seed": "node scripts/embed-mock-seed.mjs",
    "build:static": "node scripts/embed-mock-seed.mjs && node scripts/build-static.mjs",
    "validate:static": "node scripts/validate-static.mjs",
    "build": "npm run build:static && npm run validate:static"
  }
}
